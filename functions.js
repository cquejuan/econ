//Compound Interests
//A = a * (1 + r/n)^nt 
/*
A = a * (1 + r/n)^nt
=> A = a * Math.pow((1 + r/n), n * t);

a = A / (1 + r/n)^nt
=> a = A / Math.pow(1 + r/n)^nt

t = ln(A) / (nln(a * (n + r) / n))
=> t = Math.ln(a) / (n * Math.ln(a * (n + r) / n))
*/
var loader;
function create(x, y, z){

}
